<h1>All movies</h1>
<ul>
    <% @movies.each do |movie|%>
    <li><%=link_to movie.title, movie_path(movie)%></li>
    <% end %>
</ul>

<%= link_to 'Add a new movie', new_movie_path %>
<%= link_to 'Go to list of all Actors.', actors_path %>
